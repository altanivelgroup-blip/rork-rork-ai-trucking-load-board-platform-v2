// TESTING RULES - VERY PERMISSIVE FOR PHOTOUPLOADER TESTING ONLY
// TODO: RESTORE PROPER SECURITY RULES AFTER TESTING
rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // TESTING: Allow all operations for authenticated users
    match /{allPaths=**} {
      allow read: if true;
      allow write, create, update, delete: if request.auth != null;
    }
  }
}