// ULTIMATE CROSS-PLATFORM FIX: Zero-restriction storage rules for universal access
rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // ULTIMATE FIX: Universal access for all storage operations
    match /{allPaths=**} {
      allow read, write, create, update, delete: if true;
    }
  }
}